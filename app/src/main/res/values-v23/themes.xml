<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MyEyeHealth" parent="Base.Theme.MyEyeHealth">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/background_dark</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:buttonStyle">@style/Theme.MyEyeHealth.Button</item>
    </style>

    <style name="Theme.MyEyeHealth.Slider" parent="Widget.Material3.Slider">
        <item name="trackColorActive">@color/darkBlue</item>
        <item name="trackHeight">20dp</item>
        <item name="android:handle">@color/darkBlue</item>
        <item name="android:valueType">intType</item>
        <item name="android:valueFrom">0</item>
        <item name="android:valueTo">1000</item>
    </style>

    <style name="Theme.MyEyeHealth.GreenButton" parent="ThemeOverlay.Material3.Button.TonalButton">
        <item name="backgroundTint">@color/green</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.MyEyeHealth.Button" parent="Widget.Material3.Button.ElevatedButton">
        <item name="android:backgroundTint">@color/darkBlue</item>
        <item name="iconTint">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.MyEyeHealth.RedButton" parent="ThemeOverlay.Material3.Button.TonalButton">
        <item name="backgroundTint">@color/red</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>